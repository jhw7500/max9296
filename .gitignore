# Kernel module build artifacts
*.o
*.ko
*.dtb
*.mod
*.mod.c
*.mod.o
.*.cmd
modules.order
Module.symvers

# Kernel build directory
.tmp_versions/
.tmp/

# Tool/temp directories
.context/
tmp/
.bkit/
.omc/
.serena/
.vscode/

# Compound Engineering (local agent tooling — not committed to driver repo)
.compound-engineering/

# Python health exporter/test artifacts
__pycache__/
*.py[cod]
.ruff_cache/

# Board qualification backups can contain deployment/network credentials.
# Keep the local recovery copies, but never stage them as evidence.
artifacts/board-*/**/backup/
artifacts/board-*/**/edgeconf-*.json

# AI session handoff notes (internal host/path details — keep out of the driver repo)
HANDOFF.*.md

# clangd (호스트 종속 — 각자 빌드 시 생성)
compile_commands.json
.cache/
