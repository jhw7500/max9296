# MAX9296 640x360 Sensor-Readout, Crop, and 120 FPS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 카메라당 1920x1080, 1280x720, 640x360 모드를 유지하면서 640x360의 센서 readout 후보를 AP1302 공식 sensor-mode로 검증하고, 독립적인 `crop_enable`/디지털 crop 제어와 30 FPS 초과 노출 안전 차단을 구현한 뒤 PIM 보드에서 실제 120 FPS와 영상·자원 무결성을 비교한다.

**Architecture:** MAX9296 드라이버가 해상도별 AP1302 preview context, 센서-mode 시험 artifact, crop cache/cluster, 노출 사전 검증과 prepare 순서를 소유한다. gstApp은 edgeconf의 시작 설정을 정규화하고 `crop_enable`을 먼저 보낸 뒤 한 번의 `VIDIOC_S_EXT_CTRLS`로 공통 배율과 채널별 중심 tuple을 캐시하며, 세 readout 비교 Case 모두 V4L2/GStreamer 입력을 640x360으로 고정한다. sensor-mode 0~15 후보는 공개 런타임 인터페이스가 아닌 동일 소스 revision의 빌드 artifact로만 바꾸고, 보드 readback으로 SENSOR-640/HD-ISP/FHD-ISP를 분류한다.

**Tech Stack:** Linux 5.10 V4L2 subdevice controls, MAX9296/MAX9295, AP1302 preview context, AR0234 DMA readback, C kernel module, C++11/GLib/json-c/GStreamer gstApp, Bash/Python host tests, Yocto aarch64 cross-build, PIM target-board tools.

**Spec:** `docs/superpowers/specs/2026-08-28-max9296-360p-readout-120fps-design.md`

## Global Constraints

- 두 작업 트리를 그대로 사용한다.
  - Driver: `/home/jhw/ai/opencode/projects/.worktrees/max9296-360p-zoom-exposure`
  - gstApp: `/home/jhw/ai/opencode/projects/.worktrees/gstapp-360p-zoom`
- 기존 dirty 변경은 사용자 작업으로 취급한다. 삭제·reset·checkout하지 않고, 각 commit에서는 아래에 열거한 파일만 명시적으로 stage한다.
- 모든 로컬 shell 명령은 `/home/jhw/.codex/RTK.md` 계약에 따라 `rtk`로 시작한다.
- 파일 수정은 `apply_patch`로 한다. 생성물 정리 외에 `cat`, heredoc redirect, 임의 Python rewrite로 소스 파일을 쓰지 않는다.
- gstApp은 plain `make`를 사용하지 않고 `./make-for-imx8`로만 cross-build한다.
- 공개 edgeconf/V4L2 `readout_mode`와 채널별 zoom factor를 추가하지 않는다.
- 640x360은 항상 AP1302/CSI/gstApp 입력 640x360이다. gstApp downscale 비교를 추가하지 않는다.
- `crop_enable=false`이면 `0x1010`, `0x1012`, `0x118c`, `0x118e` host I2C write가 live/prepare/replay 전체에서 0회여야 한다.
- `0x1014`는 center 레지스터로 쓰지 않고, `0x510a` 쓰기는 어떤 경로에도 추가하지 않는다.
- 30 FPS 초과 자격 시험은 AE auto로 수행하고 `0x500c`를 쓰지 않는다. 수동 노출 또는 실제 exposure write가 예정되면 mode-table 첫 I2C 전에 실패한다.
- 보드 측정은 `jhw-board` skill로 PIM 예약을 확보한 뒤 시작하며, backup hash와 rollback 명령이 준비되기 전에는 배포하지 않는다.
- 녹색 화면, stride/caps 불일치, 채널 분할 손상은 blocking failure다. 해결 전에는 sensor-mode sweep이나 성능 비교를 진행하지 않는다.
- 120 FPS 합격 기준은 15~20초 안정 구간에서 raw CSI와 신뢰 가능한 ISI가 각각 118.8 FPS 이상, CSI→ISI 손실률 1% 이하, overflow/CRC/ECC/lost frame 0, 녹색/손상 프레임 0이다.

---

## File Map

### max9296 driver worktree

- Create: `max9296_360p_policy.h` — host와 kernel이 함께 사용하는 순수 sensor-mode/high-FPS 계산.
- Create: `tests/max9296_360p_policy_test.c` — reserved-bit 보존, full-FOV ROI, high-FPS fixed8 계산 시험.
- Create: `tests/run_360p_policy_test.sh` — 순수 C host test runner.
- Modify: `max9296.c` — 모드 상한, AP1302 context, crop control/cluster/cache/fingerprint, 노출 preflight, prepare/replay 순서.
- Modify: `tests/max9296_360p_zoom_exposure_test.py` — 통합 source/contract regression.
- Modify: `tests/max9296_prepare_test.py` — prepare 실패·fingerprint·출력 차단 계약.
- Modify: `tests/run_health_tests.sh` — 새 host tests 연결.
- Modify: `tools/cam_fps_stack.sh` — AP/AR readback과 machine-readable FPS/loss 결과.
- Modify: `tests/cam_fps_stack_mode_test.sh` — 360p 단일/듀얼 및 결과 포맷 시험.
- Modify: `tests/fixtures/cam_fps_stack/*` — 측정 도구 fixture 확장.
- Create: `tools/cam_360p_resource.sh` — CPU/RSS/DDR/온도/dmesg 동시 수집.
- Create: `tests/cam_360p_resource_test.sh` — fixture 기반 자원 수집 계약.
- Create: `tools/uyvy_frame_check.py` — raw UYVY 크기·luma·녹색 우세 검사.
- Create: `tests/uyvy_frame_check_test.py` — 정상/녹색/stride fixture 생성 시험.
- Create: `tools/build_360p_candidates.sh` — 같은 revision에서 sensor-mode 0~15 artifact 생성.
- Create: `tests/build_360p_candidates_test.sh` — 후보 이름·빌드 flag·manifest 계약 시험.
- Modify: `V4L2_CTRL_GUIDE.md`, `docs/v4l2-controls-guide.md`, `docs/parallel-prepare-v1.md`, `CHANGELOG.md` — 공개 계약과 복구 절차.
- Create after board evidence: `docs/360p-readout-120fps-validation.md` — 실제 register/FPS/resource/FOV 결과.

### gstApp worktree

- Modify: `max9296Controls.h`, `max9296Controls.cpp` — crop JSON 정규화, enable+tuple control plan, high-FPS exposure plan.
- Modify: `test/test_max9296Controls.cpp` — control ID/order/default/exposure plan 시험.
- Modify: `test/run-max9296-controls-test.sh` — host unit test.
- Create: `test/run-max9296-controls-source-check.sh` — parser/ioctl/prepare 순서 source contract.
- Modify: `util.h` — hardware `crop_enable[MAX_VIDEO_SRC]`; 기존 GStreamer `crop_en[2]` 유지.
- Modify: `parser.cpp` — I2C 객체의 strict boolean parse와 기본 false.
- Modify: `videoBin.cpp` — stopped-state enable ioctl 후 one-shot `VIDIOC_S_EXT_CTRLS`; 오류 전파; >30 AE-auto exposure seed 생략.
- Modify: `Makefile` — test/source dependencies가 빠지지 않도록 갱신.

### Target-only artifacts

- `/root/shared_v/edgeconf_pim.json` — backup에서 파생한 실제 배포 JSON. repository에 credential 포함 원본을 commit하지 않는다.
- `/root/camtest/backup-*` — 실행 시 UTC timestamp로 만든 module/gstApp/edgeconf 원본과 SHA-256.
- `/root/camtest/360p-results/` — Case/FPS별 register, IRQ, resource, dmesg, frame 결과.

---

## Task 1: Add the pure 360p policy with failing host tests

**Files:**

- Create: `max9296_360p_policy.h`
- Create: `tests/max9296_360p_policy_test.c`
- Create: `tests/run_360p_policy_test.sh`
- Modify: `tests/run_health_tests.sh`

- [ ] **Step 1: Write a failing pure-policy test.**

  Cover exact constants and transformations, including the two cases that prevent guessed sensor programming:

  ```c
  CHECK(MAX9296_360P_SENSOR_MODE_KEEP == 0xffU);
  CHECK(max9296_preview_sensor_mode(0xc5eaU, 2U, 5U) == 0xe5e5U);
  CHECK(max9296_preview_sensor_mode(0xffffU, 0U, 0U) == 0xcff0U);
  CHECK(max9296_preview_max_fps_fixed8(31U) == 0x1f00U);
  CHECK(max9296_preview_max_fps_fixed8(60U) == 0x3c00U);
  CHECK(max9296_preview_max_fps_fixed8(120U) == 0x7800U);
  CHECK(max9296_preview_uses_high_fps(30U) == 0U);
  CHECK(max9296_preview_uses_high_fps(31U) == 1U);
  CHECK(MAX9296_PREVIEW_ROI_X0 == 0x0000U);
  CHECK(MAX9296_PREVIEW_ROI_Y0 == 0x0000U);
  CHECK(MAX9296_PREVIEW_ROI_X1 == 0x4000U);
  CHECK(MAX9296_PREVIEW_ROI_Y1 == 0x4000U);
  CHECK(MAX9296_PREVIEW_ASPECT == 0x1000U);
  ```

- [ ] **Step 2: Run the new test and confirm RED because the header does not exist.**

  Run: `rtk bash tests/run_360p_policy_test.sh`

  Expected: compiler failure naming `max9296_360p_policy.h` or missing symbols.

- [ ] **Step 3: Implement the dependency-free policy header.**

  Use unsigned integer types so the same header compiles in a host C test and in the kernel module:

  ```c
  #ifndef MAX9296_360P_POLICY_H
  #define MAX9296_360P_POLICY_H

  #define MAX9296_360P_SENSOR_MODE_KEEP 0xffU
  #ifndef MAX9296_360P_SENSOR_MODE
  #define MAX9296_360P_SENSOR_MODE MAX9296_360P_SENSOR_MODE_KEEP
  #endif

  #if MAX9296_360P_SENSOR_MODE != MAX9296_360P_SENSOR_MODE_KEEP && \
      MAX9296_360P_SENSOR_MODE > 15
  #error "MAX9296_360P_SENSOR_MODE must be 0..15 or KEEP"
  #endif

  #define MAX9296_PREVIEW_SENSOR_MODE_CROP_MASK 0x3000U
  #define MAX9296_PREVIEW_SENSOR_MODE_INDEX_MASK 0x000fU
  #define MAX9296_PREVIEW_ROI_X0 0x0000U
  #define MAX9296_PREVIEW_ROI_Y0 0x0000U
  #define MAX9296_PREVIEW_ROI_X1 0x4000U
  #define MAX9296_PREVIEW_ROI_Y1 0x4000U
  #define MAX9296_PREVIEW_ASPECT 0x1000U

  static inline unsigned int max9296_preview_sensor_mode(
      unsigned int current, unsigned int crop_ctrl, unsigned int sensor_mode)
  {
      return (current & ~(MAX9296_PREVIEW_SENSOR_MODE_CROP_MASK |
                          MAX9296_PREVIEW_SENSOR_MODE_INDEX_MASK)) |
             ((crop_ctrl & 0x3U) << 12) |
             (sensor_mode & MAX9296_PREVIEW_SENSOR_MODE_INDEX_MASK);
  }

  static inline unsigned int max9296_preview_uses_high_fps(unsigned int fps)
  {
      return fps > 30U && fps <= 120U;
  }

  static inline unsigned int max9296_preview_max_fps_fixed8(unsigned int fps)
  {
      return fps << 8;
  }

  #endif
  ```

- [ ] **Step 4: Add the runner to the health suite and reach GREEN.**

  `tests/run_360p_policy_test.sh` must compile with `cc -std=c11 -Wall -Wextra -Werror`, use a `mktemp` binary, and remove it through `trap`.

  Run: `rtk bash tests/run_360p_policy_test.sh`

  Expected: `PASS: max9296 360p policy`.

- [ ] **Step 5: Commit only the policy foundation.**

  ```bash
  rtk git add max9296_360p_policy.h tests/max9296_360p_policy_test.c tests/run_360p_policy_test.sh tests/run_health_tests.sh
  rtk git commit -m "test: define AP1302 360p preview policy"
  ```

---

## Task 2: Program AP1302 preview context and enforce mode-specific FPS

**Files:**

- Modify: `max9296.c`
- Modify: `tests/max9296_360p_zoom_exposure_test.py`
- Modify: `tests/max9296_prepare_test.py`

- [ ] **Step 1: Extend tests first for register policy and mode limits.**

  Add assertions for:

  - `MAX9296_MODE_1280x360`, `MAX9296_MODE_640x360`, and right-hand 640 mode have `max_fps=120` and `exposure_safe_max_fps=30`.
  - HD/FHD ordinary `max_fps` remains 30.
  - frame-interval enumeration uses the selected mode's `max_fps`, not global `DEFAULT_FRAMERATE_FPS`.
  - prepare and `s_frame_interval` reject FPS above the selected mode's ordinary limit.
  - preview context writes width, height, full ROI, aspect, and only then optional sensor mode/high-FPS registers inside `0x1184` atomic begin/finish.
  - sensor-mode is a read-modify-write using mask `0x300f`; no direct AR0234 mode/timing writes are introduced.
  - candidate `KEEP` causes no `0x2014` host write.

- [ ] **Step 2: Run the focused tests and confirm RED.**

  ```bash
  rtk python3 tests/max9296_360p_zoom_exposure_test.py
  rtk python3 tests/max9296_prepare_test.py
  ```

  Expected: failures naming 360p max FPS, preview context, and sensor-mode preservation.

- [ ] **Step 3: Add named AP1302 preview registers and include the policy header.**

  Add exact definitions in `max9296.c`:

  ```c
  #include "max9296_360p_policy.h"

  #define AP1302_REG_ATOMIC 0x1184
  #define AP1302_ATOMIC_BEGIN 0x0001
  #define AP1302_ATOMIC_FINISH 0x0013
  #define AP1302_REG_PREVIEW_WIDTH 0x2000
  #define AP1302_REG_PREVIEW_HEIGHT 0x2002
  #define AP1302_REG_PREVIEW_ROI_X0 0x2004
  #define AP1302_REG_PREVIEW_ROI_Y0 0x2006
  #define AP1302_REG_PREVIEW_ROI_X1 0x2008
  #define AP1302_REG_PREVIEW_ROI_Y1 0x200a
  #define AP1302_REG_PREVIEW_ASPECT 0x200c
  #define AP1302_REG_PREVIEW_SENSOR_MODE 0x2014
  #define AP1302_REG_PREVIEW_LINE_TIME 0x201c
  #define AP1302_REG_PREVIEW_MAX_FPS 0x2020
  #define AP1302_REG_TRIGGER_MAX_MISMATCH 0x6112
  #define MAX9296_360P_MAX_FPS 120
  ```

  Do not add a write for `PREVIEW_LINE_TIME`; it stays automatic during this qualification.

- [ ] **Step 4: Raise only the single/dual 360p ordinary limit.**

  Set 1280x360, 640x360, and the right 640x360 table to `MAX9296_360P_MAX_FPS`. Keep every `exposure_safe_max_fps` at 30 and every HD/FHD `max_fps` unchanged.

  Update `max9296_s_frame_interval()`, `max9296_enum_frame_interval()`, and prepare parsing/normalization so `fps <= mode->max_fps` is checked before hardware mutation.

- [ ] **Step 5: Implement one per-AP1302 atomic context writer.**

  Preserve the first failure while always attempting atomic finish:

  ```c
  static int max9296_program_preview_context_channel(
      struct max9296_dev *sensor, u32 addr, u16 width, u16 height, u32 fps,
      unsigned int sensor_mode)
  {
      unsigned int current;
      int ret;
      int finish_ret;

      ret = maxim_ops_i2c_write(sensor, addr, AP1302_REG_ATOMIC,
                                AP1302_ATOMIC_BEGIN, 2, 2);
      if (ret)
          return ret;

  #define PREVIEW_WRITE(_reg, _val)                                      \
      do {                                                               \
          ret = maxim_ops_i2c_write(sensor, addr, (_reg), (_val), 2, 2); \
          if (ret)                                                       \
              goto finish;                                               \
      } while (0)

      PREVIEW_WRITE(AP1302_REG_PREVIEW_WIDTH, width);
      PREVIEW_WRITE(AP1302_REG_PREVIEW_HEIGHT, height);
      PREVIEW_WRITE(AP1302_REG_PREVIEW_ROI_X0, MAX9296_PREVIEW_ROI_X0);
      PREVIEW_WRITE(AP1302_REG_PREVIEW_ROI_Y0, MAX9296_PREVIEW_ROI_Y0);
      PREVIEW_WRITE(AP1302_REG_PREVIEW_ROI_X1, MAX9296_PREVIEW_ROI_X1);
      PREVIEW_WRITE(AP1302_REG_PREVIEW_ROI_Y1, MAX9296_PREVIEW_ROI_Y1);
      PREVIEW_WRITE(AP1302_REG_PREVIEW_ASPECT, MAX9296_PREVIEW_ASPECT);

      if (sensor_mode != MAX9296_360P_SENSOR_MODE_KEEP) {
          ret = maxim_ops_i2c_read(sensor, addr,
                                   AP1302_REG_PREVIEW_SENSOR_MODE,
                                   2, 2, &current);
          if (ret)
              goto finish;
          PREVIEW_WRITE(AP1302_REG_PREVIEW_SENSOR_MODE,
                        max9296_preview_sensor_mode(current, 2U,
                                                    sensor_mode));
      }

      if (max9296_preview_uses_high_fps(fps)) {
          PREVIEW_WRITE(AP1302_REG_PREVIEW_MAX_FPS,
                        max9296_preview_max_fps_fixed8(fps));
          PREVIEW_WRITE(AP1302_REG_TRIGGER_MAX_MISMATCH, 0x0000);
      }

  finish:
      finish_ret = maxim_ops_i2c_write(sensor, addr, AP1302_REG_ATOMIC,
                                       AP1302_ATOMIC_FINISH, 2, 2);
  #undef PREVIEW_WRITE
      return ret ? ret : finish_ret;
  }
  ```

  Use the programmed topology: global `0x3c` for single, `0x11` and `0x12` for dual. Per-camera output width is `mode->width / 2` for dual and `mode->width` for single.

- [ ] **Step 6: Select the compile-time sensor mode only for 640x360 output.**

  `max9296_post_firmware_program_locked()` must pass `MAX9296_360P_SENSOR_MODE` only when per-camera output is 640x360; other modes pass `KEEP`. Log adapter, channel address, output tuple, requested FPS, and compile-time mode (`KEEP` or 0~15).

  This is the only artifact difference. Do not add module params, sysfs fields, V4L2 controls, JSON keys, or direct AR0234 writes for it.

- [ ] **Step 7: Reach GREEN and prove both KEEP and candidate builds compile.**

  ```bash
  rtk python3 tests/max9296_360p_zoom_exposure_test.py
  rtk python3 tests/max9296_prepare_test.py
  rtk ./make-for-imx8
  rtk ./make-for-imx8 clean
  rtk ./make-for-imx8 KCFLAGS=-DMAX9296_360P_SENSOR_MODE=5
  ```

  Expected: tests pass; both kernel builds exit 0; the candidate build log identifies mode 5.

- [ ] **Step 8: Commit the preview-context implementation.**

  ```bash
  rtk git add max9296.c tests/max9296_360p_zoom_exposure_test.py tests/max9296_prepare_test.py
  rtk git commit -m "feat: program 360p AP1302 preview context"
  ```

---

## Task 3: Add `crop_enable`, a persistent V4L2 cluster, and zero-write disable semantics

**Files:**

- Modify: `max9296.c`
- Modify: `tests/max9296_360p_zoom_exposure_test.py`
- Modify: `tests/max9296_prepare_test.py`

- [ ] **Step 1: Write failing crop control tests.**

  Require all of the following:

  - `V4L2_CID_CROP_ENABLE = V4L2_CID_USER_BASE + 0x102b`, boolean, default 0.
  - shared cache and hardware fingerprint both contain `crop_enable`.
  - the persistent cluster contains common `dz` plus ch0/ch1 X/Y controls.
  - false returns before any call that can reach `max9296_write_zoom_channel()`.
  - true applies step, X, Y, factor-last for every active AP1302.
  - live factor/center changes reapply a full current tuple.
  - streaming enable changes return `-EBUSY` without changing cache.
  - stopped enable changes mark prepare stale; true→false does not write 1x defaults.
  - fingerprint equality includes enable state.

- [ ] **Step 2: Run the focused tests and confirm RED.**

  ```bash
  rtk python3 tests/max9296_360p_zoom_exposure_test.py
  rtk python3 tests/max9296_prepare_test.py
  ```

- [ ] **Step 3: Add control, cache, and fingerprint state.**

  Extend the existing structs with these exact ownership rules:

  ```c
  #define V4L2_CID_CROP_ENABLE (V4L2_CID_USER_BASE + 0x102b)

  struct max9296_hw_fingerprint {
      /* existing fields */
      bool crop_enable;
  };

  struct max9296_ctrls {
      /* existing fields */
      struct v4l2_ctrl *crop_enable;
      struct v4l2_ctrl *crop_cluster[5];
  };

  struct max9296_ctrl_cache {
      /* existing fields */
      bool crop_enable;
  };
  ```

  `crop_cluster` must live in `struct max9296_ctrls`; a stack-local pointer array is invalid because V4L2 keeps the cluster pointer after registration.

- [ ] **Step 4: Register and cluster the five tuple controls.**

  Register `crop_enable` as a separate boolean. After all per-channel controls exist, populate and cluster:

  ```c
  ctrls->crop_cluster[0] = ctrls->dz;
  ctrls->crop_cluster[1] = ctrls->dz_x_ch0;
  ctrls->crop_cluster[2] = ctrls->dz_y_ch0;
  ctrls->crop_cluster[3] = ctrls->dz_x_ch1;
  ctrls->crop_cluster[4] = ctrls->dz_y_ch1;
  v4l2_ctrl_cluster(ARRAY_SIZE(ctrls->crop_cluster), ctrls->crop_cluster);
  ```

  Keep common `dz_x`/`dz_y` aliases outside the cluster. Their callbacks update both channel caches and then invoke the same full-tuple apply helper.

- [ ] **Step 5: Make the cluster callback cache the entire tuple once.**

  Because clustered member updates call the master, handle `ctrl == sensor->ctrls.dz`, inspect each member's `is_new`, update all requested cache fields, and issue at most one hardware apply. Do not rely on per-member `ctrl->id` callbacks.

- [ ] **Step 6: Gate every digital-crop hardware path.**

  Rename `max9296_apply_cached_zoom()` to `max9296_apply_cached_crop()` and start it with:

  ```c
  if (!sensor->ctrl_cache.crop_enable)
      return 0;
  ```

  No caller may call `max9296_write_zoom_channel()` directly. The helper remains under `sensor->lock`, selects global vs dual addresses from programmed hardware, and logs the full public tuple and errno on failure.

- [ ] **Step 7: Implement enable state transitions.**

  In `max9296_s_ctrl()`:

  ```c
  if (ctrl->id == V4L2_CID_CROP_ENABLE) {
      bool requested = !!ctrl->val;

      if (sensor->streaming)
          return -EBUSY;
      if (sensor->ctrl_cache.crop_enable != requested) {
          sensor->ctrl_cache.crop_enable = requested;
          max9296_mark_prepare_stale_locked(sensor);
      }
      return 0;
  }
  ```

  Do not write the crop registers on either transition. Firmware reload/context initialization clears the old effect. Add the cached boolean to fingerprint normalization, equality, sysfs prepare request construction, and status output.

- [ ] **Step 8: Reach GREEN and inspect the call graph for forbidden bypasses.**

  ```bash
  rtk python3 tests/max9296_360p_zoom_exposure_test.py
  rtk python3 tests/max9296_prepare_test.py
  rtk rg -n "max9296_write_zoom_channel\(" max9296.c
  ```

  Expected: one definition and calls only inside `max9296_apply_cached_crop()`; tests pass.

- [ ] **Step 9: Commit the crop gate.**

  ```bash
  rtk git add max9296.c tests/max9296_360p_zoom_exposure_test.py tests/max9296_prepare_test.py
  rtk git commit -m "feat: gate AP1302 crop behind V4L2 enable"
  ```

---

## Task 4: Make exposure preflight and prepare/replay ordering truthful

**Files:**

- Modify: `max9296.c`
- Modify: `tests/max9296_360p_zoom_exposure_test.py`
- Modify: `tests/max9296_prepare_test.py`

- [ ] **Step 1: Add failing tests for every `0x500c` path and prepare order.**

  Test these exact cases:

  - invalid/null mode, zero FPS, or FPS above ordinary mode max → `-EINVAL`.
  - valid mode above `exposure_safe_max_fps` with a real exposure write → `-EBUSY`.
  - the rejection log includes channel, mode ID/width/height, FPS, exposure, safe cap, errno.
  - AE auto at 31/60/120 does not perform manual seed or `0x500c` and may prepare.
  - AE manual at 31/60/120 is rejected before `max9296_set_mode()`/mode-table I2C.
  - 1~30 FPS keeps manual→seed→configured-AE behavior.
  - cached-control failures propagate and keep `firmware_ready=false`.
  - successful order is preflight → mode table/firmware → preview context → optional crop → cached controls → fingerprint → output/FSYNC.
  - enable worker contains no post-output hardcoded AE/AWB/LSC rewrite.

- [ ] **Step 2: Run focused tests and confirm RED.**

  ```bash
  rtk python3 tests/max9296_360p_zoom_exposure_test.py
  rtk python3 tests/max9296_prepare_test.py
  ```

- [ ] **Step 3: Preflight the complete prepare before mode-table I2C.**

  Add `max9296_preflight_prepare_locked(sensor, fingerprint)` as the first mutation-free call in `max9296_prepare_hardware_locked()`. It validates:

  - exact mode pointer and `1 <= fps <= mode->max_fps`;
  - crop boolean and cached tuple ranges;
  - every active channel with `ae_on == 0` through `max9296_check_exposure_policy()` using the fingerprint mode/FPS rather than stale live state.

  Return immediately on error while `hardware_valid=false`, `firmware_ready=false`, MIPI disabled, and FSYNC contract unpublished.

- [ ] **Step 4: Guard direct V4L2 exposure requests even while powered off.**

  Move exposure/AE-off preflight ahead of cache mutation and remove the current `power_count > 0 && firmware_ready` condition around policy validation. Probe-time defaults remain valid because the initial mode/FPS is 30. A rejected request must neither alter the cached exposure/AE mode nor issue another I2C write.

- [ ] **Step 5: Return errors from cached control replay.**

  Change both functions to `int`:

  ```c
  static int max9296_apply_channel_controls(...);
  static int max9296_apply_cached_controls(struct max9296_dev *sensor);
  ```

  At function entry, preflight any exposure operation. For `fps > safe_max_fps` with AE auto, skip the manual-mode seed and write only configured AE auto; for `fps <= safe_max_fps`, preserve the existing manual→seed→configured-AE sequence byte-for-byte. Preserve and return the first I2C errno. Set `firmware_ready=true` only after every active channel succeeds.

- [ ] **Step 6: Put crop and controls inside prepare before fingerprint publication.**

  `max9296_prepare_hardware_locked()` must have this executable order:

  ```c
  ret = max9296_preflight_prepare_locked(sensor, fingerprint);
  if (ret)
      return ret;
  ret = max9296_set_mode(sensor, fingerprint);
  if (ret)
      return ret;
  ret = max9296_loadfw(sensor->i2c_client);
  if (ret)
      return ret;
  ret = max9296_post_firmware_program_locked(sensor, fingerprint);
  if (ret)
      return ret;
  ret = max9296_apply_cached_crop(sensor);
  if (ret)
      return ret;
  ret = max9296_apply_cached_controls(sensor);
  if (ret)
      return ret;
  sensor->initialized_fingerprint = *fingerprint;
  sensor->initialized_epoch = READ_ONCE(max9296_hw_epoch);
  sensor->hardware_valid = true;
  ```

  Every failure path leaves both validity fields cleared.

- [ ] **Step 7: Remove post-output control rewrites from the enable worker.**

  Delete the hardcoded worker writes to AE manual/auto, AWB and LSC plus the ignored `max9296_apply_cached_controls()` call after `max9296_enable_output_locked()`. The worker may reapply the latest gated crop tuple immediately before output, but false remains a no-op and any true-crop failure leaves `stream_on` pending without enabling `0x0313`.

  Make quick restart replay return and honor errors before stream commit. Do not allow any ignored return from the two cached apply helpers.

- [ ] **Step 8: Prove the centralized exposure write and forbidden-WB contracts.**

  ```bash
  rtk python3 tests/max9296_360p_zoom_exposure_test.py
  rtk python3 tests/max9296_prepare_test.py
  rtk rg -n "AP1302_REG_EXP_TIME" max9296.c
  rtk rg -ni "0x510a" max9296.c
  ```

  Expected: the test counts one direct low-level `0x500c` write in `max9296_write_exposure()`; `0x510a` search returns no match.

- [ ] **Step 9: Commit truthful prepare/replay.**

  ```bash
  rtk git add max9296.c tests/max9296_360p_zoom_exposure_test.py tests/max9296_prepare_test.py
  rtk git commit -m "fix: reject unsafe exposure before camera prepare"
  ```

---

## Task 5: Complete driver regression and candidate build tooling

**Files:**

- Create: `tools/build_360p_candidates.sh`
- Modify: `tests/run_health_tests.sh`
- Modify as failures require: `max9296.c`, `max9296_360p_policy.h`, related tests

- [ ] **Step 1: Write the candidate-builder contract first.**

  The script must:

  - refuse a dirty `max9296.c` or policy header unless `--allow-dirty` is explicitly passed;
  - record `git rev-parse HEAD`, `git diff --stat`, compiler mode and SHA-256 in `manifest.tsv`;
  - clean-build `KEEP` and modes 0~15 through `./make-for-imx8`;
  - copy each `max9296.ko` to a unique output directory as `max9296-keep.ko` and `max9296-sm00.ko` … `max9296-sm15.ko`;
  - fail immediately on a build or missing artifact; never overwrite an existing run directory.

- [ ] **Step 2: Add a shell/source test and confirm RED.**

  The test invokes `--help`, validates the enumerated artifact names without running 17 kernel builds, and checks every build command contains `KCFLAGS=-DMAX9296_360P_SENSOR_MODE=`.

  Run: `rtk bash tests/build_360p_candidates_test.sh`

- [ ] **Step 3: Implement the builder without adding a public runtime selector.**

  Keep artifact mode entirely in `KCFLAGS`; the script must not edit source between artifacts.

- [ ] **Step 4: Run all driver host and kernel gates.**

  ```bash
  rtk tests/run_health_tests.sh
  rtk bash tests/build_360p_candidates_test.sh
  rtk ./make-for-imx8 clean
  rtk ./make-for-imx8
  rtk git diff --check
  ```

  Expected: every host test passes, module build exits 0, no whitespace errors.

- [ ] **Step 5: Commit build tooling and any directly related test fixes.**

  ```bash
  rtk git add tools/build_360p_candidates.sh tests/build_360p_candidates_test.sh tests/run_health_tests.sh max9296.c max9296_360p_policy.h tests/max9296_360p_policy_test.c tests/max9296_360p_zoom_exposure_test.py tests/max9296_prepare_test.py
  rtk git commit -m "test: build isolated AP1302 sensor-mode artifacts"
  ```

---

## Task 6: Add gstApp crop configuration and pure control-plan tests

**Files:**

- Modify: `max9296Controls.h`
- Modify: `max9296Controls.cpp`
- Modify: `test/test_max9296Controls.cpp`
- Modify: `test/run-max9296-controls-test.sh`
- Modify: `util.h`
- Modify: `parser.cpp`

- [ ] **Step 1: Write failing tests for JSON normalization and ioctl plan.**

  Add cases for:

  - missing `crop_enable` → false, not an invalid-value error;
  - JSON boolean false/true → false/true;
  - integer, string, null, or non-boolean token → false with invalid flag;
  - enable control ID `0x0098192b` precedes the tuple;
  - dual tuple is common factor + ch0 X/Y + ch1 X/Y;
  - single-left/right tuple uses the active slot's center IDs;
  - factor defaults to 100 and centers to 32768;
  - >30 with all enabled channels AE auto returns `SKIP_EXPOSURE_SEED`;
  - >30 with any enabled manual AE returns `REJECT_MANUAL_EXPOSURE`;
  - <=30 returns `WRITE_EXPOSURE_SEED`.

- [ ] **Step 2: Run the host test and confirm RED.**

  Run: `rtk bash test/run-max9296-controls-test.sh`

- [ ] **Step 3: Add pure control-plan types.**

  Add to `max9296Controls.h`:

  ```cpp
  enum Max9296ExposurePlan {
      MAX9296_WRITE_EXPOSURE_SEED,
      MAX9296_SKIP_EXPOSURE_SEED,
      MAX9296_REJECT_MANUAL_EXPOSURE,
  };

  struct Max9296CropControlBatch {
      Max9296ControlWrite enable;
      Max9296ControlWrite tuple[5];
      size_t tuple_count;
  };

  uint8_t max9296_crop_enable_normalize(uint8_t present,
                                        uint8_t boolean_type,
                                        int value,
                                        uint8_t *invalid);
  int max9296_crop_build_control_batch(
      uint8_t crop_enable, uint8_t enabled_slots, uint32_t common_dz,
      const Max9296ZoomCenter centers[2], Max9296CropControlBatch *batch);
  Max9296ExposurePlan max9296_exposure_plan(uint32_t fps,
                                            uint8_t enabled_slots,
                                            uint8_t auto_ae_slots);
  ```

  `max9296_crop_build_control_batch()` always returns an enable control plus a valid cached tuple, even when enable is false. The driver, not gstApp, guarantees zero crop-register writes.

- [ ] **Step 4: Keep hardware crop state separate from GStreamer crop state.**

  Add only this field to `_CmdArg` near `dz`:

  ```cpp
  gboolean crop_enable[MAX_VIDEO_SRC];
  ```

  Keep existing `gboolean crop_en[2]` and every downstream GStreamer crop consumer unchanged.

- [ ] **Step 5: Parse strict booleans once per I2C object.**

  In `ParserClass::init_arg()`, initialize both CSI entries to false. In the even-channel branch that reads common `dz`, inspect `i2c2.crop_enable` or `i2c1.crop_enable`:

  - missing: keep false and emit one INFO/NOTICE effective-value log;
  - `json_type_boolean`: normalize the boolean;
  - every other type/value: false, increment/config-log error, continue startup.

  Continue reading `dz` from the I2C object and `dz_x`/`dz_y` from its channel objects. Reject per-channel `dz` exactly as today.

- [ ] **Step 6: Reach GREEN and verify the two crop names do not alias.**

  ```bash
  rtk bash test/run-max9296-controls-test.sh
  rtk rg -n "crop_enable|crop_en" util.h parser.cpp videoBin.cpp
  ```

  Expected: test passes; hardware and GStreamer fields have disjoint uses.

- [ ] **Step 7: Commit parser/control-plan work.**

  ```bash
  rtk git add max9296Controls.h max9296Controls.cpp test/test_max9296Controls.cpp test/run-max9296-controls-test.sh util.h parser.cpp
  rtk git commit -m "feat: parse MAX9296 hardware crop configuration"
  ```

---

## Task 7: Apply crop atomically in gstApp before prepare

**Files:**

- Modify: `videoBin.cpp`
- Modify: `max9296Controls.h`
- Modify: `max9296Controls.cpp`
- Modify: `test/test_max9296Controls.cpp`
- Create: `test/run-max9296-controls-source-check.sh`
- Modify: `Makefile`

- [ ] **Step 1: Add a failing source-order test.**

  The source check must assert:

  - `VIDIOC_S_CTRL` sends only `crop_enable` first;
  - the tuple uses one `VIDIOC_S_EXT_CTRLS`, not five `VIDIOC_S_CTRL` calls;
  - the function opens one subdevice FD, issues both ioctls, closes once, and returns errors;
  - `VideoBin::init()` returns false when crop control application fails;
  - VideoBin initialization still occurs before `max9296_prepare_all()` in `main.cpp`;
  - >30 AE-auto path does not call `V4L2_CID_EXT_TIME`;
  - >30 manual AE fails before the ordinary camera-control loop;
  - there is no gstApp scaling branch for SENSOR-640/HD-ISP/FHD-ISP.

  Run: `rtk bash test/run-max9296-controls-source-check.sh`

  Expected: failure because extended controls and error propagation are absent.

- [ ] **Step 2: Replace the per-control zoom loop with one stopped-state batch.**

  Implement a helper shaped as follows:

  ```cpp
  static int apply_crop_v4l2(int csi_num, guint8 enabled_slots,
                             guint8 ch0, guint8 ch1)
  {
      Max9296CropControlBatch batch = {};
      struct v4l2_ext_control ext[5] = {};
      struct v4l2_ext_controls controls = {};
      struct v4l2_control enable = {};
      int fd;
      int ret;

      /* Build batch from cmdArg, open /dev/v4l-subdevN once. */
      enable.id = batch.enable.id;
      enable.value = batch.enable.value;
      ret = ioctl(fd, VIDIOC_S_CTRL, &enable);
      if (ret < 0)
          goto out;

      for (size_t i = 0; i < batch.tuple_count; ++i) {
          ext[i].id = batch.tuple[i].id;
          ext[i].value = batch.tuple[i].value;
      }
      controls.which = V4L2_CTRL_WHICH_CUR_VAL;
      controls.count = batch.tuple_count;
      controls.controls = ext;
      ret = ioctl(fd, VIDIOC_S_EXT_CTRLS, &controls);
  out:
      close(fd);
      return ret;
  }
  ```

  Log CSI, enable, slot mask, common factor, both centers, `error_idx`, and errno. Do not silently continue on either ioctl error.

- [ ] **Step 3: Enforce the high-FPS exposure plan before camera controls.**

  At the start of the V4L2-control section in `VideoBin::init()`:

  - compute enabled slots and auto-AE slots;
  - `REJECT_MANUAL_EXPOSURE`: `LOG_CRIT` with CSI/FPS/channels and return false;
  - `SKIP_EXPOSURE_SEED`: omit every `V4L2_CID_EXT_TIME` call and continue AE/gain/AWB/flip setup;
  - `WRITE_EXPOSURE_SEED`: preserve existing <=30 behavior.

  Remove the duplicate single-mode `V4L2_CID_EXT_TIME` write so one plan owns it.

- [ ] **Step 4: Apply hardware crop before prepare and propagate failure.**

  Call `apply_crop_v4l2()` once for an active CSI during `VideoBin::init()`. It must complete before `main.cpp` reaches `max9296_prepare_all()`. Return false on failure so the domain is disabled/fails rather than preparing with a stale crop fingerprint.

- [ ] **Step 5: Reach GREEN with host tests and cross-compiled test binary.**

  ```bash
  rtk bash test/run-max9296-controls-test.sh
  rtk bash test/run-max9296-controls-source-check.sh
  rtk ./make-for-imx8 bin/testMax9296Controls
  rtk ./make-for-imx8 -j4
  ```

  Expected: both host contracts pass; cross-build creates `bin/testMax9296Controls` and `bin/gstApp` without new warnings treated as errors.

- [ ] **Step 6: Commit atomic gstApp control application.**

  ```bash
  rtk git add videoBin.cpp max9296Controls.h max9296Controls.cpp test/test_max9296Controls.cpp test/run-max9296-controls-source-check.sh Makefile
  rtk git commit -m "feat: apply MAX9296 crop tuple before prepare"
  ```

---

## Task 8: Extend FPS/readback/resource tools and add a green-frame gate

**Files:**

- Modify: `tools/cam_fps_stack.sh`
- Modify: `tests/cam_fps_stack_mode_test.sh`
- Modify: `tests/fixtures/cam_fps_stack/*`
- Create: `tools/cam_360p_resource.sh`
- Create: `tests/cam_360p_resource_test.sh`
- Create: `tools/uyvy_frame_check.py`
- Create: `tests/uyvy_frame_check_test.py`
- Modify: `tests/run_health_tests.sh`

- [ ] **Step 1: Add failing fixture tests for required evidence.**

  Require `cam_fps_stack.sh -D -L SENSOR-640 -R 120` to emit:

  ```text
  AP_CONTEXT case=SENSOR-640 channel=ch0 addr=0x11 width=640 height=360 roi_x0=... roi_y0=... roi_x1=... roi_y1=... aspect=... sensor_mode=... line_time=... max_fps=...
  AR_TIMING case=SENSOR-640 channel=ch0 x_start=... y_start=... x_end=... y_end=... frame_length=... line_length=... read_mode=... exposure=...
  FPS_RESULT case=SENSOR-640 requested=120 channel=ch0 sensor=... isp=... csi=... isi=... loss_pct=... isi_trust=0|1 pass120=0|1
  ```

  Add single 640x360 and dual 1280x360 fixtures and wrap-around frame-counter data at 120 FPS.

- [ ] **Step 2: Confirm RED.**

  ```bash
  rtk bash tests/cam_fps_stack_mode_test.sh
  rtk bash tests/cam_360p_resource_test.sh
  rtk python3 tests/uyvy_frame_check_test.py
  ```

- [ ] **Step 3: Extend deep AP1302 and AR0234 readback.**

  Read every active AP address:

  - AP1302: `0x2000`, `0x2002`, `0x2004`, `0x2006`, `0x2008`, `0x200a`, `0x200c`, `0x2014`, `0x201c`, `0x2020`.
  - AR0234 through DMA: `0x3002`, `0x3004`, `0x3006`, `0x3008`, `0x300a`, `0x300c`, `0x3012`, `0x3040`.

  Keep current human-readable table and append stable key/value lines. Add `-L/--label` and `-R/--requested-fps`; do not infer Case names from sensor-mode values.

- [ ] **Step 4: Calculate a strict machine-readable result.**

  Use actual elapsed nanoseconds. `pass120=1` only when requested=120, CSI>=118.8, trusted ISI>=118.8, and loss<=1.0%. If the existing CSI/ISI raw IRQ ratio marks ISI untrustworthy, emit `isi_trust=0` and `pass120=0`; do not substitute caps FPS.

- [ ] **Step 5: Add resource collection with capability detection.**

  `cam_360p_resource.sh` must run for a fixed duration and record:

  - gstApp PID, `/proc/PID/status` VmRSS, process CPU ticks;
  - aggregate `/proc/stat` CPU utilization;
  - `perf stat` DDR cycles/read-cycles/write-cycles when `imx8_ddr0` exposes them;
  - all `/sys/class/thermal/thermal_zone*/temp` values before/after;
  - dmesg delta and counts for `overflow|crc|ecc|lost frame|timeout|green`;
  - media/V4L2 width, height, pixel format, bytesperline and sizeimage.

  Missing DDR PMU is a reported `ddr_supported=0`, not fabricated zero utilization; production resource comparison remains incomplete until a supported counter is available.

- [ ] **Step 6: Add raw UYVY integrity analysis.**

  `uyvy_frame_check.py` accepts width, height, bytesperline and raw path, verifies exact frame/stride size, converts sampled UYVY pixels to RGB, reports luma variance and green-dominant ratio, and exits nonzero for constant/mostly-green or truncated data. Synthetic tests must include valid color bars, >80% green, and short-stride frames.

- [ ] **Step 7: Reach GREEN and shell-parse all tools.**

  ```bash
  rtk bash -n tools/cam_fps_stack.sh tools/cam_360p_resource.sh tests/cam_fps_stack_mode_test.sh tests/cam_360p_resource_test.sh
  rtk bash tests/cam_fps_stack_mode_test.sh
  rtk bash tests/cam_360p_resource_test.sh
  rtk python3 tests/uyvy_frame_check_test.py
  rtk tests/run_health_tests.sh
  ```

- [ ] **Step 8: Commit measurement gates.**

  ```bash
  rtk git add tools/cam_fps_stack.sh tools/cam_360p_resource.sh tools/uyvy_frame_check.py tests/cam_fps_stack_mode_test.sh tests/cam_360p_resource_test.sh tests/uyvy_frame_check_test.py tests/fixtures/cam_fps_stack tests/run_health_tests.sh
  rtk git commit -m "test: measure 360p sensor ISP and frame integrity"
  ```

---

## Task 9: Document the interface and a deployable edgeconf mutation

**Files:**

- Modify: `V4L2_CTRL_GUIDE.md`
- Modify: `docs/v4l2-controls-guide.md`
- Modify: `docs/parallel-prepare-v1.md`
- Modify: `CHANGELOG.md`
- Create: `docs/examples/edgeconf-max9296-640x360-fragment.json`

- [ ] **Step 1: Document the public resolution/crop contract.**

  Include the exact single/dual table, crop ID/ranges/defaults, corrected AP1302 mapping, 8.8 examples for 100/150/200/300, false zero-write rule, runtime tuple behavior, runtime enable `-EBUSY`, and hard-reset requirement to clear old crop state.

- [ ] **Step 2: Document FPS/exposure behavior.**

  State that 640x360 may negotiate up to the board-qualified limit, exposure writes remain capped at 30, >30 AE auto omits seed, manual exposure is rejected, and `0x510a` is intentionally absent.

- [ ] **Step 3: Add a schema fragment, not a guessed credential-bearing full config.**

  The fragment must be valid JSON and contain both I2C domains:

  ```json
  {
    "width": 640,
    "height": 360,
    "fps": 120,
    "i2c2": {
      "crop_enable": false,
      "dz": 100,
      "ch0": { "dz_x": 32768, "dz_y": 32768 },
      "ch1": { "dz_x": 32768, "dz_y": 32768 }
    },
    "i2c1": {
      "crop_enable": false,
      "dz": 100,
      "ch2": { "dz_x": 32768, "dz_y": 32768 },
      "ch3": { "dz_x": 32768, "dz_y": 32768 }
    }
  }
  ```

  Explain that deployment uses `jq` to merge these keys into the board's existing `.VHL_CAM` object so unrelated settings and credentials remain untouched.

- [ ] **Step 4: Validate docs and JSON.**

  ```bash
  rtk python3 -m json.tool docs/examples/edgeconf-max9296-640x360-fragment.json
  rtk rg -n "0x1014|0x510a|crop_enable|118c|118e" V4L2_CTRL_GUIDE.md docs/v4l2-controls-guide.md docs/parallel-prepare-v1.md CHANGELOG.md
  rtk git diff --check
  ```

- [ ] **Step 5: Commit documentation.**

  ```bash
  rtk git add V4L2_CTRL_GUIDE.md docs/v4l2-controls-guide.md docs/parallel-prepare-v1.md CHANGELOG.md docs/examples/edgeconf-max9296-640x360-fragment.json
  rtk git commit -m "docs: define 360p readout crop and exposure contract"
  ```

---

## Task 10: Run the complete host/cross-build gate in both repositories

**Files:**

- Verify only; modify the smallest owning file when a gate reveals a defect.

- [ ] **Step 1: Verify driver repository state and tests.**

  ```bash
  rtk git status --short
  rtk tests/run_health_tests.sh
  rtk ./make-for-imx8 clean
  rtk ./make-for-imx8
  rtk git diff --check
  ```

- [ ] **Step 2: Verify forbidden register and direct-write invariants.**

  ```bash
  rtk rg -ni "0x510a" max9296.c
  rtk rg -n "maxim_ops_i2c_write\([^;]*AP1302_REG_EXP_TIME" max9296.c
  rtk rg -n "AP1302_REG_DZ_CUR_FCT|0x1014" max9296.c
  ```

  Expected: no `0x510a`; exactly one low-level exposure write; no center write to `0x1014`.

- [ ] **Step 3: Verify gstApp from its worktree.**

  ```bash
  rtk bash test/run-max9296-controls-test.sh
  rtk bash test/run-max9296-controls-source-check.sh
  rtk ./make-for-imx8 clean
  rtk ./make-for-imx8 -j4
  rtk git diff --check
  rtk file bin/gstApp bin/testMax9296Controls
  ```

  Expected: host tests pass; both binaries are aarch64 artifacts; no plain host build is used.

- [ ] **Step 4: Review cross-repository tuple consistency.**

  Confirm exact control IDs, defaults, 640/1280/1920 single/dual widths, FPS limit semantics, and JSON names are identical in tests/docs/source. Confirm gstApp has no sensor Case selector and no app downscale branch.

- [ ] **Step 5: Commit only gate-driven fixes, separately per repository.**

  Use a narrow `fix:` commit in the owning repository; do not combine driver and gstApp histories or stage unrelated dirty files.

---

## Task 11: Reserve PIM, back up the target, deploy KEEP baseline, and clear the green-screen gate

**Files:**

- Target-only deployment files and evidence
- Create after evidence: driver `docs/360p-readout-120fps-validation.md`

- [ ] **Step 1: Invoke the `jhw-board` skill and reserve the PIM target.**

  Record the returned board identity, holder, reservation expiry, and SSH endpoint in the validation document. Stop if the board cannot be exclusively reserved.

- [ ] **Step 2: Discover exact installed paths without mutation.**

  Through the reserved SSH endpoint, record:

  ```bash
  rtk ssh "$PIM_HOST" 'modinfo -n max9296; systemctl show -p ExecStart cam-operate.service; pgrep -a gstApp; ls -l /root/shared_v/edgeconf_pim.json; sha256sum /root/shared_v/edgeconf_pim.json'
  ```

  The known original edgeconf SHA-256 to compare is `b075884b59ce0d44b3361bd7ae37b0fa4bb04da2c96b94bddf4d9deb42447a6f`.

- [ ] **Step 3: Create a timestamped recoverable backup before deployment.**

  Copy the installed module, running gstApp binary, edgeconf, service status, `lsmod`, media graph and SHA-256 manifest into one `/root/camtest/backup-*` directory. Verify each copied file hash matches its source and record the backup directory in the validation doc.

- [ ] **Step 4: Build and deploy the KEEP module and current gstApp.**

  Build the driver with the default `KEEP` policy and gstApp from the verified host revisions. Transfer to run-specific temporary names, verify hashes on target, stop `cam-operate`, install with original owner/mode, run `depmod -a`, and use `/root/camtest/cam_hard_reset.sh -s -S` or `/opt/pim/bin/init_cam.sh`. Do not rely on gstApp restart alone.

- [ ] **Step 5: Merge the actual edgeconf keys safely.**

  First assert `.VHL_CAM`, `.VHL_CAM.i2c2`, and any active `.VHL_CAM.i2c1` are objects. Use `jq` against the backup to set width=640, height=360, fps=30, `crop_enable=false`, dz=100 and all center values=32768 while preserving every unrelated key. Validate with `jq -e`, atomically install the result, and save its SHA-256.

- [ ] **Step 6: Hard-reset and verify the exact media contract.**

  Record `media-ctl -p`, `v4l2-ctl --get-fmt-video`, subdevice format, width, height, UYVY fourcc, bytesperline and sizeimage. Dual output must be 1280x360 with per-channel boundary at 640; single output must be 640x360. UYVY requires bytesperline consistent with two bytes per pixel unless the driver reports documented padding.

- [ ] **Step 7: Gate the observed green screen before any performance run.**

  Capture raw frames with gstApp stopped and the same prepared tuple, run `uyvy_frame_check.py`, then decode one RTSP frame with gstApp running and inspect it. Use this decision boundary:

  - raw invalid/green → fix driver context, ISI format/stride, or dual width before continuing;
  - raw valid but RTSP green → fix gstApp source caps, conversion, or channel split before continuing;
  - both valid → save frame hashes/images and proceed.

  Check journal/dmesg for UYVY, caps negotiation, stride, overflow, CRC/ECC and lost-frame evidence. No FPS result is accepted while either image path is mostly green or geometrically split incorrectly.

- [ ] **Step 8: Re-run host gates after any green-screen fix and commit it in the owning repository.**

  A board-only workaround is not acceptable. Add a reproducing host/source test, fix the owning driver or gstApp file, rebuild, redeploy, hard-reset and repeat Steps 6–7 until clean.

---

## Task 12: Sweep documented AP1302 sensor modes and classify three full-FOV artifacts

**Files:**

- Generated host artifacts from `tools/build_360p_candidates.sh`
- Target evidence under `/root/camtest/360p-results/`
- Modify: `docs/360p-readout-120fps-validation.md`

- [ ] **Step 1: Build one manifest-backed candidate set from one clean revision.**

  Run the builder once and retain KEEP plus SM00~SM15 artifacts. Verify the manifest shows the same source revision and only `MAX9296_360P_SENSOR_MODE` differs.

- [ ] **Step 2: Run each candidate at 640x360@30 with crop disabled.**

  For every candidate:

  1. stop service;
  2. install candidate module and verify hash;
  3. run hard reset;
  4. start the same gstApp/edgeconf;
  5. collect AP context and both AR timing readbacks;
  6. verify UYVY/stride and raw+RTSP green gate;
  7. repeat one restart/hard-reset to check deterministic readback.

  A timeout, SoC hang, mismatched channel timing, central-only crop, or damaged image marks that mode rejected. Recover with the backed-up KEEP module before the next candidate.

- [ ] **Step 3: Classify from measurements, never from the mode number.**

  Assign labels only when readback and FOV prove them:

  - `SENSOR-640`: full 16:9 FOV, verified bin/subsample/read-mode and 640x360-equivalent sensor workload;
  - `HD-ISP`: full FOV 1280x720 sensor readout followed by 2:1 AP rescale;
  - `FHD-ISP`: full FOV 1920x1080 sensor readout followed by 3:1 AP rescale, reproducing the prior dz=100 baseline.

  AR window size alone is insufficient; include `0x3040`, line/frame timing, image FOV target and AP context. Central 640x360 window is a crop result and cannot be `SENSOR-640`.

- [ ] **Step 4: Enforce the qualification stop condition.**

  If three deterministic full-FOV profiles cannot be identified, stop the comparison, restore all original artifacts and document which profiles were unavailable. Do not guess direct AR0234 timing, hide dz=300, or invent a public selector.

- [ ] **Step 5: Preserve selected artifacts and evidence.**

  Copy the three selected module files, manifest rows, AP/AR readbacks, media graphs and image hashes into the results directory. Record their exact sensor-mode values in the validation document.

---

## Task 13: Run the 30/60/120 sensor-vs-ISP matrix and resource comparison

**Files:**

- Target results
- Modify: `docs/360p-readout-120fps-validation.md`

- [ ] **Step 1: Freeze fair conditions.**

  Use the same cameras, channel mask, lens target, illumination, AE/AWB/gain, gstApp binary, encoder/RTSP settings, 640x360 caps, 20-second measurement duration, and thermal stabilization interval. Set `crop_enable=false`, dz=100, centers=32768. Change only selected module artifact and requested FPS; hard-reset between every artifact/FPS pair.

- [ ] **Step 2: Run all nine Case/FPS combinations.**

  For SENSOR-640, HD-ISP and FHD-ISP at 30, 60 and 120:

  - merge only `.VHL_CAM.fps` into the validated edgeconf;
  - hard-reset with service restart;
  - run `cam_fps_stack.sh -D -d 20 -i 2 -L CASE -R FPS`;
  - run `cam_360p_resource.sh` for the same stable window;
  - capture raw and decoded image samples;
  - save dmesg/journal deltas and register readbacks.

- [ ] **Step 3: Reject misleading caps-only success.**

  Record requested/caps FPS separately from sensor, AP HINF, CSI and ISI results. `videorate`, RTSP caps, or encoder output at 120 does not pass a Case whose raw path is below 118.8.

- [ ] **Step 4: Apply strict 120 FPS and image gates.**

  Mark 120 supported only when both channels meet all numeric thresholds, ISI is trustworthy, register/timing is symmetric, no transport errors occur, and every raw/RTSP sample passes the green/stride check. Prior values around 113~115 FPS are explicitly failures, not rounded 120.

- [ ] **Step 5: Compare only the intended resource boundary.**

  Tabulate for every Case/FPS:

  - sensor active/read mode, line/frame length and theoretical FPS;
  - AP input workload classification and output 640x360;
  - sensor/AP/CSI/ISI actual FPS and loss;
  - gstApp CPU/RSS, system CPU, DDR read/write/cycles, temperatures;
  - sharpness, noise, aliasing, FOV and green ratio.

  Explain that MIPI/ISI/gstApp workload should be similar because all outputs are 640x360; differences are expected mainly before AP1302 output.

- [ ] **Step 6: Repeat the winning 120 Case three times.**

  Require three independent hard-reset runs with no FPS, image or transport failure before selecting a production default.

---

## Task 14: Run independent crop, exposure, resolution, and restart regressions

**Files:**

- Target results
- Modify: `docs/360p-readout-120fps-validation.md`

- [ ] **Step 1: Prove false sends zero digital-crop writes.**

  Place a kernel log marker, hard-reset and run each public resolution in single and dual configurations with crop false. Search the delta for exact `reg=0x1010`, `reg=0x1012`, `reg=0x118c`, `reg=0x118e`; all counts must be zero through prepare, STREAMON, gstApp restart and firmware replay.

- [ ] **Step 2: Verify crop true at all three resolutions.**

  At 1920x1080, 1280x720 and 640x360, single and dual where available:

  - set stopped `crop_enable=true` and hard-reset/prepare;
  - apply dz=100/150/200 and distinct ch0/ch1 centers at runtime with one comma-separated `v4l2-ctl --set-ctrl` call;
  - confirm the driver writes step→X→Y→factor-last for each active AP address;
  - confirm output resolution stays unchanged and each channel moves independently with one common factor.

- [ ] **Step 3: Verify enable cannot change while streaming.**

  A live `crop_enable=0` or `1` transition must fail with EBUSY, leave cache/hardware unchanged and keep the stream healthy. After STREAMOFF, the transition must cache and mark prepare stale without writing default crop registers.

- [ ] **Step 4: Verify hard-reset persistence and gstApp-restart distinction.**

  With crop true, hard-reset/firmware reload must reapply JSON tuple. A gstApp-only restart with unchanged enable may reuse matching hardware. A true→false change without hardware epoch reset must not pretend the old crop vanished; use `cam_hard_reset.sh -s -S` and then prove false zero writes.

- [ ] **Step 5: Verify exposure guard at both safe and unsafe FPS.**

  - 640x360@120 + AE auto: stream succeeds and no `0x500c` delta appears.
  - direct `exp_time` or channel exposure at 120: ioctl returns EBUSY; no I2C write.
  - manual AE pending before 120 prepare: prepare returns EBUSY before the first mode-table write.
  - invalid mode/FPS: EINVAL with full rejection log.
  - 30 FPS manual exposure/gain/AE: existing writes and image response still work.
  - source and target log contain no new `0x510a` write.

- [ ] **Step 6: Run existing prepare/health smoke gates on target.**

  Exercise cold prepare, warm reuse, gstApp restart, hard reset and the existing board prepare gate without leaving the service or reservation in an unexpected state.

---

## Task 15: Select the qualified production limit, deploy, document, or roll back

**Files:**

- Modify if qualified result demands it: `max9296_360p_policy.h`, `max9296.c`, driver tests
- Modify: `docs/360p-readout-120fps-validation.md`, `CHANGELOG.md`
- Target production module/gstApp/edgeconf

- [ ] **Step 1: Convert board evidence into the production constants.**

  If SENSOR-640 passes every gate, set the default `MAX9296_360P_SENSOR_MODE` to its measured index and keep `MAX9296_360P_MAX_FPS=120`. If no Case passes 120, set the production 360p ordinary max to the highest repeatedly qualified FPS and state that 120 is unsupported. If no full-FOV sensor-readout candidate exists, retain the verified KEEP/ISP path rather than a central crop.

- [ ] **Step 2: Update tests with the measured production decision.**

  Assert the final default sensor mode, final ordinary max FPS and the retained exposure cap. Keep candidate build overrides available for reproducibility.

- [ ] **Step 3: Re-run both full host/cross-build gates.**

  ```bash
  rtk tests/run_health_tests.sh
  rtk ./make-for-imx8 clean
  rtk ./make-for-imx8
  ```

  In the gstApp worktree:

  ```bash
  rtk bash test/run-max9296-controls-test.sh
  rtk bash test/run-max9296-controls-source-check.sh
  rtk ./make-for-imx8 clean
  rtk ./make-for-imx8 -j4
  ```

- [ ] **Step 4: Commit the evidence-based production decision.**

  Driver commit:

  ```bash
  rtk git add max9296_360p_policy.h max9296.c tests/max9296_360p_policy_test.c tests/max9296_360p_zoom_exposure_test.py docs/360p-readout-120fps-validation.md CHANGELOG.md
  rtk git commit -m "feat: qualify production 640x360 sensor mode"
  ```

  If gstApp required a board-reproduced green-screen fix, commit that separately in gstApp with its reproducing test.

- [ ] **Step 5: Deploy exactly one production build and JSON.**

  Transfer the final module and gstApp using hashes, merge the validated 640x360/crop settings into the original edgeconf, install atomically, run `depmod -a`, hard-reset with service restart, and execute the winning 120 smoke plus all three resolution/crop-false smoke tests.

- [ ] **Step 6: Hand the user the exact test procedure.**

  Report:

  - deployed module/gstApp/config paths and SHA-256;
  - edgeconf keys for width/height/fps/crop_enable/dz/centers;
  - hard-reset command required after enable/mode changes;
  - `cam_fps_stack.sh` command and pass thresholds;
  - runtime crop tuple command and expected `-EBUSY` enable behavior;
  - rollback directory and command;
  - SENSOR-640 vs HD-ISP vs FHD-ISP resource/image conclusion.

- [ ] **Step 7: Roll back on any failed production smoke.**

  Restore module, gstApp and edgeconf from the recorded backup, verify all three original SHA-256 values, run `cam_hard_reset.sh -s -S`, confirm `cam-operate.service` and original stream health, and document the failed gate. Never leave a partially selected artifact installed.

- [ ] **Step 8: Release the PIM board reservation.**

  Use `jhw-board` to release the exact reservation and verify no stale holder/lease remains.

---

## Final Verification Checklist

- [ ] Public per-camera modes are exactly 1920x1080, 1280x720, 640x360; dual widths are exactly doubled.
- [ ] All sensor/ISP comparison outputs and gstApp inputs are 640x360; no app downscale comparison exists.
- [ ] No public `readout_mode`, no hidden dz=300 readout substitution, and no direct AR0234 production timing writes exist.
- [ ] `crop_enable=false` produces zero writes to all four digital-crop registers.
- [ ] `crop_enable=true` supports runtime factor/center tuple updates, factor-last, and streaming enable EBUSY.
- [ ] Crop state is in cache and fingerprint; stopped transitions stale prepare and hard reset clears prior hardware crop.
- [ ] Every `0x500c` write uses the guarded helper; unsafe manual/pending exposure fails before I2C; safe behavior is retained.
- [ ] AP1302 `0x510a` is absent.
- [ ] Prepare publishes fingerprint only after context, optional crop and all cached controls succeed.
- [ ] KEEP and modes 0~15 are reproducible artifacts from one revision; labels come from AP/AR/FOV evidence.
- [ ] Green/stride/channel-split integrity passes before FPS results are considered.
- [ ] 120 support is claimed only with repeated >=118.8 raw-path FPS, <=1% loss and zero transport/image failures.
- [ ] CPU/RSS/system CPU/DDR/temperature and sharpness/noise/aliasing/FOV are reported for all three Cases.
- [ ] Final target artifacts and edgeconf hashes are recorded, rollback is proven, service state is healthy, and the PIM reservation is released.

## Self-Review Record

- Spec coverage: sections 3–13 are mapped to Tasks 2–15 and the Final Verification Checklist.
- Completion-marker audit: production sensor-mode와 보드 합격값을 미리 채우지 않았다. 런타임 값은 reservation/build/readback manifest에서 명시적으로 파생한다.
- Type audit: kernel-facing values use `u32/u16/bool`; the shared pure header uses dependency-free `unsigned int`; gstApp ABI values use fixed-width C++ integers and convert to V4L2 `int32_t` only at ioctl construction.
- Safety audit: target mutation is preceded by exclusive reservation, exact path discovery, verified backup and rollback; every artifact/FPS change requires a hard reset; green screen and unsupported PMU/readback stop completion claims.
